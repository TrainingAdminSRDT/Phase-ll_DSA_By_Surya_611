
// 7. Reverse Integer
class Solution {
    public int reverse(int x) {
        int reverse = 0;

        while (x != 0) {
            int lastDigit = x % 10;
            reverse = reverse * 10 + lastDigit;
            x = x / 10;
        }

        return reverse;
    }
}
    
