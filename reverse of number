#include <iostream>
using namespace std;

int main() {
    int num, rev = 0;

    cout << "Enter a number: ";
    cin >> num;

    while (num != 0) {
        int rem = num % 10;
        rev = rev * 10 + rem;
        num /= 10;
    }

    cout << "Reverse = " << rev << endl;

    return 0;
}
